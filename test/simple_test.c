#include <stdio.h>

int main() {
    printf("Testing basic compilation without calling imported functions...\n");
    printf("If this compiles and runs, the import library format is being recognized!\n");
    printf("Generated library with real COFF import objects!\n");
    return 0;
}
